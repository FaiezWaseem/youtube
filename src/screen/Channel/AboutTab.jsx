import { Box, Text } from "rn-faiez-components"


export default function AboutTab({ channelId, navigation }) {

    return <Box flex={1} >
        <Text color={'white'} fontSize={22} >Not Available Now</Text>
    </Box>
}